<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dg.s1.bankbook.BankbookDAO">

	<select id="getCount" resultType="java.lang.Long" parameterType="com.dg.s1.util.Pager">
		SELECT COUNT(BOOKNUMBER) FROM BANKBOOK
		WHERE
		<choose>
			<when test="kind == 'num'">
				BOOKNUMBER
			</when>

			<when test="kind == 'name'">
				BOOKNAME
			</when>

			<otherwise>
				BOOKRATE
			</otherwise>

		</choose>
		LIKE '%' || #{search} || '%'
	</select>


	<update id="setUpdate"
		parameterType="com.dg.s1.bankbook.BankbookDTO">
		UPDATE BANKBOOK SET bookName=#{bookName},
		bookRate=#{bookRate}, bookSale=#{bookSale}
		WHERE
		bookNumber=#{bookNumber}
	</update>

	<delete id="setDelete" parameterType="java.lang.Long">
		DELETE BANKBOOK WHERE
		BOOKNUMBER=#{bookNumber}
	</delete>

	<insert id="setInsert"
		parameterType="com.dg.s1.bankbook.BankbookDTO">
		INSERT INTO BANKBOOK (bookNumber, bookName, bookRate,
		bookSale)
		VALUES (bankbook_seq.nextval, #{bookName}, #{bookRate},
		#{bookSale})
	</insert>

	<select id="getList" resultType="com.dg.s1.bankbook.BankbookDTO"
		parameterType="com.dg.s1.util.Pager">
		SELECT * FROM
		(SELECT rownum R,
		BK.* FROM
		(SELECT *
		FROM BANKBOOK
		WHERE

		<choose>
			<when test="kind == 'num'">
				BOOKNUMBER
			</when>

			<when test="kind == 'name'">
				BOOKNAME
			</when>

			<otherwise>
				BOOKRATE
			</otherwise>

		</choose>

		LIKE '%' || #{search} || '%'
		ORDER BY BOOKNUMBER DESC
		)
		BK
		)
		WHERE R
		BETWEEN #{startRow} AND #{lastRow}
	</select>

	<select id="getSelect"
		parameterType="com.dg.s1.bankbook.BankbookDTO"
		resultType="com.dg.s1.bankbook.BankbookDTO">
		SELECT * FROM BANKBOOK WHERE BOOKNUMBER=#{bookNumber}
	</select>



</mapper>